import { createAIProvider } from "./providers";

export function createAiProvider() {
  return createAIProvider();
}

export type {
  AIProvider,
  AiProvider,
  AssistantMessage,
  AssistantRequest,
  AssistantResponse,
} from "./types";
